# 债务标题

状态：<未销账 | 触发中>（一句话现状）

**现象**

现在是什么样。给可核查的定位（`路径`、`路径:行号`、命令输出）。

**影响**

谁会在什么时候撞上，撞上时看到什么。

**触发条件**

什么情况下必须先还（例如：动到 X 之前、发布 Y 之前）。写「触发中」就要说清眼下卡在哪。

**销账条件**

Done when：一句话、可判定。

**不修的理由**

现在不做的成本或时机依据。没有理由就不是债，是疏漏。
